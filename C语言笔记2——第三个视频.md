#  C语言笔记2——第三个视频

## A授课与时间

### A1讲述了大学学习地应用与结果导向

### A2论证了AI的应用技巧对个人竞争力的提升意义

### A3开始授课

## B表达式、

### 一元

+ 正号运算符+（自加）
- 负号运算符-（自减）

- ### 二元
+ +加法运算符
+ -减法运算符
+ / 除法运算符
+ % 求余运算符
+ *乘法运算符
+ ps：
+ int 型和 float 型的结果是 float 9+2.5f = 11.5 6.7f/2 =3.35
  % 操作不能是浮点数，只能是整数
  / 和 % 用作负数时，结果难以确定
  -9/7  -9%7
  -1or-2  -2or5
  有的向 0 取整
  有的向无穷取整

## B运算符的优先级和结合性
是默认有的
a+bc = a+(bc)
无需纠结，直接加 () 有的先算

## C赋值运算符
### C1把=右边的值赋给左边
左值
12=i ×
i+j=2 ×
-i=j ×
i=j
i=-j
i=-j+4-k

只有出现在等号左边才会被改变 从右向左一一赋值

### C3表达简化

一下一样

i=i+2;
i+=2;

a+=2; //等价于a=a+2

a+=b a=a+b

## D自增运算符和自减运算符

i=i+1 i++【先赋再算】 ++i【先算再赋】

## E关系运算符

< >= <=
结果只有真假，真 = 1，假 = 0
6<7 是 1
6>7 是 0
2，判等运算符
5==5 是 1（等于）
5!=5 是 0（不等于）
3，逻辑运算符
或且非
!            &&      ||
逻辑非 逻辑与 逻辑或
逻辑与（&&）
真 && 真 = 真
真 && 假 = 假
假 && 假 = 假
逻辑或（||）
真 || 真 = 真
真 || 假 = 真
假 || 假 = 假
!真 && 真 =假

！假&&假=真

## F选择语句
1，关系运算符

if(表达式)
    语句
else
    语句
    ps二者后只能有一个语句 可以用{}化多为一
      这是一种串联的连续判断结构

if(a<b)
{
    a
=5;
}
if(b<c)
{
    a
=3;
}
else
{
    a
=2;
}

else永远属于上一个if

if可以独立出现

## G switch与连续判断

nt main()
{
    printf("请输入你的成绩：");
    scanf("%d",&num);
    switch(num)
    {
        case 0:
            printf("您的成绩不合格");
            break;
        case 1:
            printf("您的成绩不合格");
        case 2:
            printf("您的成绩不合格");
        case 3:
            printf("您的成绩不合格");
        case 4:
            printf("您的成绩不合格");
        case 5:
            printf("您的成绩不合格");
        case 6:
            printf("您的成绩合格");
        case 7:
            printf("您的成绩优秀");
switch:这是一个连续的判定，它后面的括号中是判定的变量
case:意义为如果switch的变量等于case后的数字就执行以下的所有
break:case产生的连续执行于此终止
# 对C语言的学习1

## A授课与时间

### A1论证了打字的重要性

### A2论证教授了文件整理的意义与方法及数字+分类

### A3以red panda为平台围绕两个程序开始授课

## B程序与所学内容

### B1你好wic：printf（“hello wic”\nhallow may）

1printf：输出

2（）：输出的范围

3“”：输出的内容
4\n：转行
5\\n：\

### B2体积的运算：

#include <stdio.h>   //预处理指令之一
#include <stdlib.h>
int lenth=5;
int width=3;
int high=4;
int volum;
int a=3;
float b=3.14;
int main(int argc, char *argv[]) 
    printf("请输入物体的长: ");  
    scanf("%d",&lenth);         
printf("请输入物体的宽: ");
scanf("%d",&width);

printf("请输入物体的高: ");
scanf("%d",&high);

volum=lenth*width*high;

printf("物体的体积是：%d",volum);

return 0;
1 #include <stdio.h>   
#include <stdlib.h>
：预处理指令
2int：整数变量
3float：浮点变量
4＝：赋值
5＋：加
6-：减
7*：乘
8/：除
9%：取余
10int main(int argc, char *argv[]) ：唯一的所运行的运算为一下内容，主函数
11scanf：输入
12%d：这有个后面的整数变量
13%f：这有个有个后面的小数变量
14&：在输入的时候必须要加的取地址，再出现%x对整形或辅导眼型的指代时候
15//or/*------*/：注释
16define：宏定义 就是

### B3printf("a=%9.5d\nb=%9.2f",a,b);

1.：之前是.前精度之后是点后精度

## C通用的格式

1 #include <stdio.h>   
#include <stdlib.h>
：预处理指令
2声明变量有什么？
3在主函数下面写东西
4一个return

## D常见的问题

1编时必须用英语符号
2%x必须需要在数量与种类上面对应，后面的变量
3大写和小写本质上是两个字母
4变量不要在开头前面写数字，只能是数字字母加下划线

## E想到的问题

Char应该用什么？

答%c

